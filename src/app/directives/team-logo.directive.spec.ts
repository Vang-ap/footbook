import { TeamLogoDirective } from './team-logo.directive';

describe('TeamLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new TeamLogoDirective();
    expect(directive).toBeTruthy();
  });
});
